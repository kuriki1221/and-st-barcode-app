# JANバーコードラベル自動生成Webアプリ 仕様メモ

## 背景・目的
サンクラウドのEC業務で、ZOZOTOWN委託返却品にJANバーコードラベルを貼る作業を自動化する。
従来は「andSTデータ確認→JANサイトでバーコード生成→ラベル屋さんに手動で貼付→印刷」という
手作業だったが、これをWebアプリ1つで完結させたい。

## 全体フロー
1. ユーザーが②ZOZO委託返却リスト(CSV)をブラウザからアップロード
2. アプリが対象4ブランドのみに絞り込み
   - 対象ブランド（②の「親カテゴリ」列で判定）: `ap retro`, `sakishimatokyo`, `LAC-VERT`, `B.R.U`
3. ①andST商品登録データ(xlsx)と自動照合してJANコードを特定
4. 照合できたものはバーコードラベルPDFを自動生成
5. 照合できなかったものは「要確認リスト」としてCSV出力
6. 生成したPDFをGoogleドライブの指定フォルダへ自動保存
7. 社内の複数人・複数PCがブラウザ経由でアクセスできるようにする

## 入力ファイル仕様

### ①andST商品登録データ(xlsx) - マスタデータ
- 決まった場所に置いておき、アプリが自動参照する想定（アップロード不要）
- シート名: `xlsxWorkbook`
- 使用する列: `メーカー品番`, `カラー名称`, `サイズ名称`, `JAN`
- 注意点:
  - `メーカー品番`は複数コードがスラッシュ区切りで入ることがある（例: `AP23055/AP23136/AP25165`）→ 文字列としてそのまま完全一致で使う
  - `カラー名称`はカラーコードが末尾に付く。付き方が不統一:
    - 括弧付き: `アイボリー(06)`
    - 括弧なし・末尾2桁数字: `アイボリー06`, `ブラック系その他2 13`（スペース0〜2個+2桁コード）
    - カラーコードを除去した「カラー基本名」は、括弧内除去 → 末尾の `\s*\d{2}$` を除去、の2段階で作る

### ②ZOZO委託返却リスト(csv) - 毎回アップロード
- 文字コード: **cp932 (Shift-JIS)** ※UTF-8では読めない
- 使用する列: `親カテゴリ`(ブランド判定), `ブランド品番`, `カラー`, `サイズ`, `数量`, `商品名`

## 照合ロジック（①のメーカー品番・カラー・サイズ ×②のブランド品番・カラー・サイズ）

サイズは以下のマップで正規化してから比較する:
```
LARGE/L → L, MEDIUM/M → M, SMALL/S → S,
X-LARGE/XLARGE/XL → XL, XX-LARGE/XXLARGE/XXL → XXL,
FREE/F/ONE SIZE → FREE
```

カラーは次の優先順位で照合する:
1. ②のカラー名をそのまま①の「カラー基本名」と完全一致で照合
2. 一致しなければ、②のカラー名の末尾の数字（例: `ブルー系その他2` の `2`）を除去した名前で再照合
   - ただし、同一(ブランド品番,サイズ)内で、この「末尾数字を除いた名前」に複数の異なる生カラー値がぶら下がっている場合（例: 同じ品番・サイズ内に`ブルー系その他1`と`ブルー系その他2`が両方存在）は、**あいまいと判断し自動照合せず要確認リストへ**
3. いずれも一致しなければ「要確認リスト」へ（理由: 品番・カラー・サイズが①に見つからない）

## ラベルPDF仕様

### 用紙: A-one L24AM500N
- シートサイズ: A4 (210mm × 297mm)
- 面付け: 24面（3列 × 8段）
- 1片サイズ: 70mm × 33.9mm
- 上下余白あり、左右余白なし（3×70mm=210mmでちょうど埋まる）

### 1ラベルのレイアウト（実物見本で確認済み）
- セル内、左側約30mmにテキストエリア、右側にバーコード
- テキスト: `メーカー品番-カラー名称-サイズ名称` を自動改行で表示（フォントサイズ8pt程度）
  - 例: `SA21212/SA26073-ブルー系その他(45)-FREE`
- バーコード: JAN(EAN-13)、人間可読文字（13桁数字）付き
- 1PDFページ = 1SKU（同じ内容を24面すべてに repeat）
- 複数SKUがある場合は、SKUごとにページを分ける

### 技術メモ（プロトタイプで検証済み・動作確認OK）
- バーコード生成: `reportlab.graphics.barcode.eanbc.Ean13BarcodeWidget`（追加ライブラリ不要、外部JAN生成サイトも不要）
- 日本語フォント: `reportlab.pdfbase.cidfonts.UnicodeCIDFont('HeiseiKakuGo-W5')`（追加インストール不要）
- PDF生成: `reportlab.pdfgen.canvas`

## 出力
- ラベルPDF（複数SKU分、SKUごとにページ分割）
- 要確認リストCSV（ブランド品番・カラー・サイズ・商品名・理由）
- 保存先: Googleドライブの指定フォルダへ自動アップロード（Google Drive API連携が必要）

## 未確定事項（Claude Codeで構築時に確認・決定すること）
- [ ] ①andSTマスタxlsxの実際の格納場所・更新方法
- [ ] Googleドライブの保存先フォルダ（フォルダID）
- [ ] Google Drive APIの認証方式（サービスアカウント/OAuth等）
- [ ] アプリのホスティング先（社内で複数人がアクセスできる環境）
- [ ] 実行トリガー: ユーザーが都度アプリを開いて手動実行（自動スケジュール実行は不要）

## 参考: 元の手作業フロー（自動化前）
1. andSTから商品情報をDL
2. ZOZOTOWNで委託返却したandST納品用のデータをDL
3. シール作成フォーマットに、andSTに登録する品番をコピペ
4. andSTの商品情報データから新規品番登録する商品を検索、JANコードを確認
5. JAN生成ページでJAN生成し、シール作成フォーマットに貼り付け
6. シール作成フォーマットから品番とバーコード部分のみPDF化
7. PDF化したものをズームアップしスクリーンショット
8. スクリーンショットを「ラベル屋さん」に貼り付け、比率など変更
9. 印刷→PDF書き出しで「品番-カラー名-サイズ」保存
10. PCに保存、andSTのバーコードデータフォルダに保存

→ 上記3〜10を、①②のデータから全自動で実行するのが今回のアプリの目的。